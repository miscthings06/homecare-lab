---
import Layout from '../../layouts/Layout.astro';
---
<Layout
  title="홈케어 비용 계산기 — 입주청소·렌탈·전기요금 예상 비용"
  description="입주청소 예상비용, 렌탈 vs 구매 손익분기점, 가전제품별 전기요금을 직접 계산해보세요. 평수·조건별 세부 입력으로 내 상황에 맞는 비용 기준을 정확하게 확인합니다."
>

<!-- 카테고리 히어로 -->
<section class="category-hero">
  <div class="container">
    <div class="breadcrumb" style="margin-bottom:14px"><a href="/">홈</a><span>›</span><strong>계산기</strong></div>
    <h1 style="font-size:clamp(1.5rem,3.5vw,2.1rem);font-weight:800;color:var(--primary);margin-bottom:12px;line-height:1.3">홈케어 비용 계산기</h1>
    <p style="max-width:600px;color:var(--text-muted);font-size:1rem;line-height:1.7;margin-bottom:0">평수·사용 기간·세부 조건을 입력하면 예상 비용을 즉시 확인할 수 있습니다. 계산 결과는 참고용이며 실제 비용은 견적으로 확인하세요.</p>
    <div class="cat-stats">
      <span class="cat-stat">🏠 입주청소 예상비용</span>
      <span class="cat-stat">🔄 렌탈 vs 구매 손익분기</span>
      <span class="cat-stat">⚡ 가전 전기요금 계산</span>
    </div>
  </div>
</section>

<div class="container" style="max-width:820px;padding-top:40px;padding-bottom:80px">

  <!-- 탭 네비게이션 -->
  <div class="calc-tabs" role="tablist">
    <button class="calc-tab active" data-tab="ipju" role="tab">🏠 입주청소</button>
    <button class="calc-tab" data-tab="rental" role="tab">🔄 렌탈 vs 구매</button>
    <button class="calc-tab" data-tab="jeongi" role="tab">⚡ 전기요금</button>
  </div>

  <!-- ① 입주청소 계산기 -->
  <section id="ipju" class="calc-panel active">

    <!-- 사용 가이드 (항상 펼쳐진 상태) -->
    <div class="calc-guide">
      <div class="calc-guide-header">📖 이 계산기 사용 방법</div>
      <div class="calc-guide-body">
        <ol>
          <li><strong>평수 선택</strong> — 등기부등본 또는 분양 면적 기준으로 선택하세요. 청소 업체는 보통 <em>전용면적</em>을 기준으로 합니다.</li>
          <li><strong>주거 형태 선택</strong> — 아파트 기준 가격이 가장 높고, 빌라·오피스텔은 구조상 다소 낮게 책정됩니다.</li>
          <li><strong>공실·거주 상태</strong> — 짐이 없는 공실 상태에서 청소하면 효율이 높아 비용이 절감됩니다.</li>
          <li><strong>이전 거주 기간</strong> — 오랜 기간 거주한 집일수록 기름때·오염이 누적돼 비용이 올라갑니다.</li>
          <li><strong>추가 항목</strong> — 창틀·베란다·에어컨·후드는 기본 견적에 포함되지 않는 경우가 많습니다. 반드시 확인하세요.</li>
        </ol>
        <p class="guide-tip">💡 <strong>팁:</strong> 같은 조건이라도 업체마다 비용이 크게 다를 수 있습니다. 계산 결과를 기준 예산으로 사용하되, 최소 2~3곳 견적을 비교하는 것을 권장합니다.</p>
      </div>
    </div>

    <div class="calc-card">
      <h2 class="calc-section-title">🏠 입주청소 예상비용 계산기</h2>
      <p class="calc-desc">평수·주거형태·상태·오염도·추가 항목을 선택하면 예상 비용 범위를 계산합니다.</p>

      <div class="calc-form">
        <div class="calc-row">
          <div class="calc-field">
            <label for="ipju-size">평수 선택 <span class="required">*</span></label>
            <select id="ipju-size">
              <option value="">-- 평수 선택 --</option>
              <option value="10">10평 이하 (원룸)</option>
              <option value="15">15평 이하</option>
              <option value="20">20평</option>
              <option value="25">25평</option>
              <option value="30">30평</option>
              <option value="33">33평</option>
              <option value="40">40평</option>
              <option value="50">50평 이상</option>
            </select>
          </div>
          <div class="calc-field">
            <label for="ipju-type">주거 형태</label>
            <select id="ipju-type">
              <option value="apt">아파트</option>
              <option value="villa">빌라·다세대</option>
              <option value="officetel">오피스텔</option>
            </select>
          </div>
        </div>

        <div class="calc-row">
          <div class="calc-field">
            <label for="ipju-status">청소 당시 상태</label>
            <select id="ipju-status">
              <option value="1">공실 상태 (짐 없음)</option>
              <option value="1.1" selected>거주 중 (짐 있음)</option>
            </select>
          </div>
          <div class="calc-field">
            <label for="ipju-years">이전 거주 기간</label>
            <select id="ipju-years">
              <option value="0.9">신축·입주 전 (1년 미만)</option>
              <option value="1" selected>1~5년</option>
              <option value="1.2">5~10년</option>
              <option value="1.5">10년 이상</option>
            </select>
          </div>
        </div>

        <div class="calc-field">
          <label>오염 정도</label>
          <div class="radio-group">
            <label class="radio-item"><input type="radio" name="ipju-dirty" value="1" checked> 보통 (일반 생활 흔적)</label>
            <label class="radio-item"><input type="radio" name="ipju-dirty" value="1.3"> 심한 편 (기름때·묵은 때)</label>
            <label class="radio-item"><input type="radio" name="ipju-dirty" value="1.6"> 매우 심함 (오랜 공실·방치)</label>
          </div>
        </div>

        <div class="calc-field">
          <label>흡연 여부</label>
          <div class="radio-group">
            <label class="radio-item"><input type="radio" name="ipju-smoke" value="1" checked> 비흡연 (담배 냄새 없음)</label>
            <label class="radio-item"><input type="radio" name="ipju-smoke" value="1.25"> 흡연 (벽면 오염·냄새 있음, 비용 상향)</label>
          </div>
        </div>

        <div class="calc-field">
          <label>추가 항목 선택 (복수 선택 가능)</label>
          <div class="checkbox-group">
            <label class="check-item"><input type="checkbox" id="add-changtul" value="55000"> 창틀·창문 청소 (+5.5만원)</label>
            <label class="check-item"><input type="checkbox" id="add-veranda1" value="25000"> 베란다 1개 (+2.5만원)</label>
            <label class="check-item"><input type="checkbox" id="add-veranda2" value="25000"> 베란다 2개 이상 (+5만원 추가)</label>
            <label class="check-item"><input type="checkbox" id="add-aircon" value="75000"> 에어컨 청소 1대 (+7.5만원)</label>
            <label class="check-item"><input type="checkbox" id="add-aircon2" value="60000"> 에어컨 추가 1대 (+6만원)</label>
            <label class="check-item"><input type="checkbox" id="add-hood" value="55000"> 주방 후드 청소 (+5.5만원)</label>
            <label class="check-item"><input type="checkbox" id="add-mold" value="90000"> 욕실 곰팡이 제거 (+9만원)</label>
            <label class="check-item"><input type="checkbox" id="add-saetak" value="80000"> 세탁기 통세척 (+8만원)</label>
            <label class="check-item"><input type="checkbox" id="add-mattress" value="60000"> 매트리스 청소 (+6만원)</label>
          </div>
        </div>
      </div>

      <div class="calc-btn-row">
        <button class="calc-run-btn" id="ipju-calc-btn">예상 비용 계산하기</button>
        <button class="calc-reset-btn" id="ipju-reset-btn" type="button">초기화</button>
      </div>

      <div class="calc-result-box" id="ipju-result" style="display:none">
        <div class="result-range" id="ipju-range"></div>
        <div class="result-detail" id="ipju-detail"></div>
        <p class="result-note">실제 비용은 업체·지역·현장 상태에 따라 달라집니다. 정확한 비용은 현장 견적으로 확인하세요.</p>
        <div class="result-cta-wrap">
          <p class="result-cta-label">이 예산 범위에서 입주청소 업체를 무료로 비교해보세요</p>
          <a href="https://ajd.co.kr" rel="nofollow sponsored noopener" data-partner-cta="true" class="result-cta-btn">
            무료 견적받기 →
          </a>
          <p class="result-cta-note">* 제휴 링크 · 이용자 추가 비용 없음 · <a href="/affiliate-disclosure/" style="color:inherit;text-decoration:underline">제휴 고지</a></p>
        </div>
        <button class="result-retry-btn" id="ipju-retry-btn">↩ 다시 계산하기</button>
      </div>
    </div>

    <div class="calc-info-box">
      <h3>입주청소 비용이 달라지는 핵심 요인</h3>
      <ul>
        <li>같은 33평이라도 오염도·추가 항목에 따라 비용이 2배 이상 차이 날 수 있습니다.</li>
        <li>창틀·베란다·에어컨·후드는 기본 견적에 포함되지 않는 경우가 대부분입니다.</li>
        <li>공실 상태 청소는 짐이 있는 상태보다 효율이 높아 비용이 낮게 책정됩니다.</li>
        <li>흡연 집은 벽면·가구 오염이 강해 기본 요금에 20~30% 추가 적용됩니다.</li>
        <li>10년 이상 거주한 집은 기름때·묵은 오염이 누적돼 고오염 기준이 적용됩니다.</li>
      </ul>
    </div>
  </section>

  <!-- ② 렌탈 vs 구매 계산기 -->
  <section id="rental" class="calc-panel">

    <!-- 사용 가이드 (항상 펼쳐진 상태) -->
    <div class="calc-guide">
      <div class="calc-guide-header">📖 이 계산기 사용 방법</div>
      <div class="calc-guide-body">
        <ol>
          <li><strong>제품 선택</strong> — 제품을 선택하면 평균 렌탈료·구매가·유지비가 자동으로 채워집니다. 직접 수정도 가능합니다.</li>
          <li><strong>월 렌탈료</strong> — 기본 요금만 입력하세요. 설치비·이전 설치비는 별도입니다.</li>
          <li><strong>연간 유지비</strong> — 구매 시 자가 관리 비용입니다. 정수기 필터 비용(연 10~18만원)처럼 직접 부담하는 소모품·수리비를 입력하세요.</li>
          <li><strong>가입 방법</strong> — 비교채널 경유 가입 시 평균 20만원 혜택이 렌탈 비용에서 차감 반영됩니다.</li>
          <li><strong>약정 후 소유권</strong> — 무상 이전 계약이라면 약정 만료 후 제품이 내 것이 됩니다. 이를 반영해 장기 비용을 계산합니다.</li>
        </ol>
        <p class="guide-tip">💡 <strong>팁:</strong> 렌탈과 구매의 비용 차이가 30만원 이내라면 관리 편의성·고장 대응·최신 모델 교체 등 비금전적 요소도 함께 고려하세요.</p>
      </div>
    </div>

    <div class="calc-card">
      <h2 class="calc-section-title">🔄 렌탈 vs 구매 손익분기 계산기</h2>
      <p class="calc-desc">제품 종류·렌탈료·약정 기간·구매 가격을 입력하면 어느 시점부터 구매가 유리한지 계산합니다.</p>

      <!-- 제품 프리셋 -->
      <div class="calc-field" style="margin-bottom:20px">
        <label>제품 빠른 선택 (자동 입력)</label>
        <div class="preset-grid">
          <button class="preset-btn" data-fee="39900" data-months="60" data-buy="1800000" data-maint="150000">💧 정수기</button>
          <button class="preset-btn" data-fee="25000" data-months="48" data-buy="600000" data-maint="80000">🌬️ 공기청정기</button>
          <button class="preset-btn" data-fee="18000" data-months="60" data-buy="300000" data-maint="20000">🚿 비데</button>
          <button class="preset-btn" data-fee="55000" data-months="60" data-buy="1200000" data-maint="0">🌀 건조기</button>
          <button class="preset-btn" data-fee="50000" data-months="60" data-buy="1500000" data-maint="0">🧊 냉장고</button>
          <button class="preset-btn" data-fee="45000" data-months="60" data-buy="1000000" data-maint="0">🫧 세탁기</button>
        </div>
      </div>

      <div class="calc-form">
        <div class="calc-row">
          <div class="calc-field">
            <label for="rental-fee">월 렌탈료 (원) <span class="required">*</span></label>
            <input type="text" id="rental-fee" placeholder="예: 39,900" inputmode="numeric">
          </div>
          <div class="calc-field">
            <label for="rental-months">약정 기간</label>
            <select id="rental-months">
              <option value="36">36개월 (3년)</option>
              <option value="48">48개월 (4년)</option>
              <option value="60" selected>60개월 (5년)</option>
              <option value="72">72개월 (6년)</option>
            </select>
          </div>
        </div>

        <div class="calc-row">
          <div class="calc-field">
            <label for="buy-price">제품 구매 가격 (원) <span class="required">*</span></label>
            <input type="text" id="buy-price" placeholder="예: 1,800,000" inputmode="numeric">
          </div>
          <div class="calc-field">
            <label for="annual-maintenance">구매 시 연간 유지비 (원)</label>
            <input type="text" id="annual-maintenance" placeholder="필터·수리 등 (없으면 0)" inputmode="numeric" value="0">
          </div>
        </div>

        <div class="calc-field">
          <label>가입 방법</label>
          <div class="radio-group">
            <label class="radio-item"><input type="radio" name="rental-channel" value="0" checked> 브랜드 직접 가입 (혜택 없음)</label>
            <label class="radio-item"><input type="radio" name="rental-channel" value="200000"> 비교채널 경유 가입 (평균 20만원 혜택 반영)</label>
          </div>
        </div>

        <div class="calc-field">
          <label>약정 만료 후 소유권 이전</label>
          <div class="radio-group">
            <label class="radio-item"><input type="radio" name="rental-ownership" value="0" checked> 소유권 이전 없음 (반납)</label>
            <label class="radio-item"><input type="radio" name="rental-ownership" value="1"> 무상 소유권 이전 (제품이 내 것)</label>
            <label class="radio-item"><input type="radio" name="rental-ownership" value="2"> 소액 지불 후 이전 (약 5~10만원)</label>
          </div>
        </div>
      </div>

      <div class="calc-btn-row">
        <button class="calc-run-btn" id="rental-calc-btn">손익분기 계산하기</button>
        <button class="calc-reset-btn" id="rental-reset-btn" type="button">초기화</button>
      </div>

      <div class="calc-result-box" id="rental-result" style="display:none">
        <div class="result-range" id="rental-range"></div>
        <div id="rental-infographic"></div>
        <div class="result-detail" id="rental-detail"></div>
        <p class="result-note">위약금·소모품 교체 빈도 등 개인 조건에 따라 달라질 수 있습니다.</p>
        <div class="result-cta-wrap" id="rental-cta-wrap"></div>
        <button class="result-retry-btn" id="rental-retry-btn">↩ 다시 계산하기</button>
      </div>
    </div>

    <div class="calc-info-box">
      <h3>렌탈 vs 구매 — 이런 경우 각각 유리합니다</h3>
      <ul>
        <li><strong>렌탈이 유리:</strong> 초기 목돈 부담 줄이고 싶을 때, 필터·고장 관리를 맡기고 싶을 때, 이사가 잦을 때</li>
        <li><strong>구매가 유리:</strong> 5년 이상 장기 사용 확정, 직접 필터 교체 가능, 초기 비용 여유 있을 때</li>
        <li><strong>비교채널 경유 렌탈:</strong> 동일 조건에서 평균 20만원 현금혜택으로 렌탈 실질 비용을 낮출 수 있습니다.</li>
        <li><strong>무상 소유권 이전 계약:</strong> 약정 후 제품이 내 것이 되므로 장기적으로 구매와 유사한 효과입니다.</li>
      </ul>
    </div>
  </section>

  <!-- ③ 전기용품 전기요금 계산기 -->
  <section id="jeongi" class="calc-panel">

    <!-- 사용 가이드 (항상 펼쳐진 상태) -->
    <div class="calc-guide">
      <div class="calc-guide-header">📖 이 계산기 사용 방법</div>
      <div class="calc-guide-body">
        <ol>
          <li><strong>가전제품 선택</strong> — 제품을 선택하면 평균 소비전력과 사용 패턴이 자동으로 채워집니다. 직접 수정도 가능합니다.</li>
          <li><strong>소비전력(W)</strong> — 제품 뒷면 라벨이나 설명서에서 확인하세요. 모르면 프리셋 선택 후 그대로 사용하세요.</li>
          <li><strong>하루 사용 시간</strong> — 냉장고처럼 24시간 켜두는 제품은 24시간으로 입력하세요.</li>
          <li><strong>월 사용 일수</strong> — 에어컨·전기히터처럼 계절성 기기는 실제 사용 일수를 입력하세요.</li>
          <li><strong>전기요금 단가</strong> — 현재 가구의 월 사용량 구간에 맞는 단가를 선택하세요. 고사용 구간일수록 누진 요금이 높아집니다.</li>
        </ol>
        <p class="guide-tip">💡 <strong>팁:</strong> 여러 가전제품의 월 요금을 각각 계산한 뒤 합산하면 가정 내 전기 소비 현황을 파악하는 데 도움이 됩니다.</p>
      </div>
    </div>

    <div class="calc-card">
      <h2 class="calc-section-title">⚡ 전기용품 전기요금 계산기</h2>
      <p class="calc-desc">에어컨·전기히터·건조기 등 가전제품의 소비전력과 사용 패턴을 입력하면 월 추가 전기요금을 추정합니다.</p>

      <!-- 가전제품 프리셋 -->
      <div class="calc-field" style="margin-bottom:20px">
        <label>가전제품 빠른 선택 (자동 입력)</label>
        <div class="preset-grid elec-preset-grid">
          <button class="preset-btn elec-preset" data-watt="1500" data-hours="6" data-days="30" data-tip="냉방">❄️ 에어컨</button>
          <button class="preset-btn elec-preset" data-watt="1500" data-hours="8" data-days="30" data-tip="난방">🔥 전기히터·온풍기</button>
          <button class="preset-btn elec-preset" data-watt="100" data-hours="8" data-days="30" data-tip="난방">🛏️ 전기장판</button>
          <button class="preset-btn elec-preset" data-watt="2000" data-hours="1.5" data-days="25" data-tip="건조">🌀 건조기</button>
          <button class="preset-btn elec-preset" data-watt="160" data-hours="24" data-days="30" data-tip="냉장">🧊 냉장고</button>
          <button class="preset-btn elec-preset" data-watt="700" data-hours="1" data-days="25" data-tip="조리">🍚 전기밥솥·전자레인지</button>
          <button class="preset-btn elec-preset" data-watt="150" data-hours="4" data-days="30" data-tip="영상">📺 TV</button>
          <button class="preset-btn elec-preset" data-watt="35" data-hours="12" data-days="30" data-tip="공조">💨 공기청정기</button>
          <button class="preset-btn elec-preset" data-watt="800" data-hours="1.2" data-days="20" data-tip="세탁">🫧 세탁기</button>
        </div>
      </div>

      <div class="calc-form">
        <div class="calc-row">
          <div class="calc-field">
            <label for="elec-watt">소비전력 (W) <span class="required">*</span></label>
            <select id="elec-watt">
              <option value="35">35W — 공기청정기·소형 선풍기</option>
              <option value="100">100W — 전기장판·소형 TV</option>
              <option value="160">160W — 냉장고 (일반형)</option>
              <option value="300">300W — 중형 TV·전자레인지 (저출력)</option>
              <option value="700">700W — 전기밥솥·소형 에어컨·소형 전열기</option>
              <option value="800">800W — 세탁기 (일반)</option>
              <option value="1000">1000W — 전기온풍기 (소형)·중형 에어컨</option>
              <option value="1500" selected>1500W — 전기히터·중대형 에어컨</option>
              <option value="2000">2000W — 대형 에어컨·건조기 (소형)</option>
              <option value="2500">2500W — 대형 건조기·대형 에어컨</option>
            </select>
          </div>
          <div class="calc-field">
            <label for="elec-watt-custom">소비전력 직접 입력 (W, 선택사항)</label>
            <input type="number" id="elec-watt-custom" placeholder="예: 1200 (직접 입력 시 우선 적용)" min="1" max="10000">
          </div>
        </div>

        <div class="calc-row">
          <div class="calc-field">
            <label for="elec-hours">하루 평균 사용 시간 (시간)</label>
            <input type="number" id="elec-hours" value="6" min="0.5" max="24" step="0.5">
          </div>
          <div class="calc-field">
            <label for="elec-days">월 사용 일수 (일)</label>
            <input type="number" id="elec-days" value="30" min="1" max="31">
          </div>
        </div>

        <div class="calc-field">
          <label for="elec-price">전기요금 단가 (원/kWh)</label>
          <select id="elec-price">
            <option value="120">약 120원 — 낮은 누진 구간 (월 200kWh 이하)</option>
            <option value="185" selected>약 185원 — 일반 평균 (월 200~400kWh)</option>
            <option value="280">약 280원 — 높은 누진 구간 (월 400kWh 초과)</option>
          </select>
        </div>

        <div class="calc-field" id="elec-tip-box" style="display:none">
          <div class="elec-tip-banner" id="elec-tip-text"></div>
        </div>
      </div>

      <div class="calc-btn-row">
        <button class="calc-run-btn" id="jeongi-calc-btn">전기요금 계산하기</button>
        <button class="calc-reset-btn" id="jeongi-reset-btn" type="button">초기화</button>
      </div>

      <div class="calc-result-box" id="jeongi-result" style="display:none">
        <div class="result-range" id="jeongi-range"></div>
        <div class="result-detail" id="jeongi-detail"></div>
        <p class="result-note" id="jeongi-note">실제 전기요금은 가구별 전체 사용량·누진 구간에 따라 달라집니다. 이 값은 해당 가전제품 단독 추가분입니다.</p>
        <a href="/jeolryak/" class="result-cta-btn" style="background:var(--primary)">생활비 절감 정보 보러가기 →</a>
        <button class="result-retry-btn" id="jeongi-retry-btn">↩ 다시 계산하기</button>
      </div>
    </div>

    <div class="calc-info-box">
      <h3>가전제품별 전기요금 절약 포인트</h3>
      <ul>
        <li><strong>에어컨:</strong> 설정 온도 1도 올리면 약 7% 절약, 선풍기 병행 시 체감 온도 2~3도 낮아짐</li>
        <li><strong>전기히터·온풍기:</strong> 인버터형이 정속형 대비 20~40% 전력 절감, 가동 시간 최소화 중요</li>
        <li><strong>건조기:</strong> 저온 건조 모드 활용 시 전력 15~25% 절감, 필터 청결이 효율에 직결됨</li>
        <li><strong>냉장고:</strong> 문 개방 빈도·냉장고 위치(직사광선 유무)에 따라 소비전력이 20% 이상 차이</li>
        <li><strong>세탁기:</strong> 냉수 세탁 전환 시 온수 가열 전력 절감, 가득 채워서 1번 vs 소량 여러 번 큰 차이</li>
        <li><strong>대기전력:</strong> TV·전자레인지 등 미사용 시 콘센트 뽑으면 월 3,000~5,000원 절약 가능</li>
      </ul>
    </div>
  </section>

</div>

<style>
/* 탭 */
.calc-tabs { display: flex; gap: 8px; margin-bottom: 24px; flex-wrap: wrap; }
.calc-tab {
  padding: 10px 18px; border: 2px solid var(--border); background: #fff;
  border-radius: 8px; font-size: 0.9rem; font-weight: 700; color: var(--text-muted);
  cursor: pointer; transition: all 0.15s; font-family: var(--font);
}
.calc-tab:hover { border-color: var(--accent); color: var(--accent); }
.calc-tab.active { border-color: var(--accent); background: var(--accent); color: #fff; }
.calc-panel { display: none; }
.calc-panel.active { display: block; }

/* 사용 가이드 — 항상 펼쳐진 고정 블록 */
.calc-guide {
  background: #f7fbf9;
  border: 1px solid rgba(47,191,155,0.25);
  border-radius: 10px;
  margin-bottom: 16px;
  overflow: hidden;
}
.calc-guide-header {
  padding: 14px 18px;
  font-size: 0.88rem;
  font-weight: 700;
  color: var(--accent-dark);
  border-bottom: 1px solid rgba(47,191,155,0.2);
  background: rgba(47,191,155,0.06);
}
.calc-guide-body {
  padding: 16px 20px;
  font-size: 0.86rem;
  color: var(--text-muted);
  line-height: 1.7;
}
.calc-guide-body ol { padding-left: 20px; margin: 0 0 12px; }
.calc-guide-body li { margin-bottom: 6px; }
.guide-tip {
  background: rgba(47,191,155,0.08);
  border-radius: 8px;
  padding: 10px 14px;
  margin: 0;
  font-size: 0.84rem;
  color: var(--primary);
}

/* 카드·폼 */
.calc-card {
  background: #fff; border: 1px solid var(--border);
  border-radius: 14px; padding: 28px; margin-bottom: 20px;
}
.calc-section-title { font-size: 1.15rem; font-weight: 800; color: var(--primary); margin-bottom: 8px; }
.calc-desc { font-size: 0.9rem; color: var(--text-muted); margin-bottom: 24px; line-height: 1.6; }
.calc-form { display: flex; flex-direction: column; gap: 18px; margin-bottom: 24px; }
.calc-row { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }
.calc-field { display: flex; flex-direction: column; gap: 6px; }
.calc-field > label { font-size: 0.88rem; font-weight: 700; color: var(--text); }
.required { color: var(--accent); }
.calc-field select,
.calc-field input[type="number"],
.calc-field input[type="text"] {
  padding: 10px 14px; border: 1.5px solid var(--border); border-radius: 8px;
  font-size: 0.93rem; background: #fff; font-family: var(--font); color: var(--text);
  transition: border-color 0.15s; width: 100%; box-sizing: border-box;
}
.calc-field select:focus, .calc-field input:focus { outline: none; border-color: var(--accent); }
.radio-group, .checkbox-group { display: flex; flex-direction: column; gap: 8px; }
.radio-item, .check-item {
  display: flex; align-items: flex-start; gap: 8px;
  font-size: 0.87rem; color: var(--text); cursor: pointer; font-weight: 400; line-height: 1.5;
}
.radio-item input, .check-item input { accent-color: var(--accent); width: 16px; height: 16px; flex-shrink: 0; margin-top: 2px; }

/* 제품 프리셋 */
.preset-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 8px; }
.elec-preset-grid { grid-template-columns: repeat(3, 1fr); }
.preset-btn {
  padding: 9px 8px; border: 1.5px solid var(--border); background: #fff;
  border-radius: 8px; font-size: 0.82rem; font-weight: 700; color: var(--text-muted);
  cursor: pointer; font-family: var(--font); transition: all 0.15s; text-align: center;
}
.preset-btn:hover { border-color: var(--accent); color: var(--accent-dark); background: rgba(47,191,155,0.06); }
.preset-btn.selected { border-color: var(--accent); background: rgba(47,191,155,0.1); color: var(--accent-dark); }

/* 전기 팁 배너 */
.elec-tip-banner {
  background: rgba(255,193,7,0.1);
  border: 1px solid rgba(255,193,7,0.35);
  border-radius: 8px;
  padding: 10px 14px;
  font-size: 0.84rem;
  color: #7a5a00;
  line-height: 1.6;
}

/* 버튼 행 */
.calc-btn-row { display: flex; gap: 10px; align-items: center; margin-bottom: 4px; }
.calc-run-btn {
  flex: 1; max-width: 380px; padding: 14px;
  background: var(--accent); color: #fff; border: none;
  border-radius: 10px; font-size: 1rem; font-weight: 800;
  cursor: pointer; font-family: var(--font); transition: background 0.15s, transform 0.1s;
}
.calc-run-btn:hover { background: var(--accent-dark); transform: translateY(-1px); }
.calc-reset-btn {
  padding: 12px 18px; background: #fff; color: var(--text-muted);
  border: 1.5px solid var(--border); border-radius: 10px;
  font-size: 0.88rem; font-weight: 600; cursor: pointer;
  font-family: var(--font); transition: all 0.15s;
}
.calc-reset-btn:hover { border-color: var(--primary); color: var(--primary); }

/* 결과 박스 */
.calc-result-box {
  margin-top: 20px; background: var(--primary);
  border-radius: 12px; padding: 24px; color: #fff;
  text-align: center; animation: fadeIn 0.3s ease;
}
@keyframes fadeIn { from { opacity:0; transform: translateY(8px); } to { opacity:1; transform: translateY(0); } }
.result-range { font-size: 1.55rem; font-weight: 900; color: var(--accent); margin-bottom: 8px; line-height: 1.3; }
.result-detail { font-size: 0.88rem; color: rgba(255,255,255,0.75); margin-bottom: 10px; line-height: 1.7; }
.result-note { font-size: 0.76rem; color: rgba(255,255,255,0.45); margin-bottom: 16px; line-height: 1.5; }
.result-cta-wrap { margin-bottom: 16px; }
.result-cta-label { font-size: 0.87rem; color: rgba(255,255,255,0.85); margin-bottom: 10px; font-weight: 600; }
.result-cta-note { font-size: 0.72rem; color: rgba(255,255,255,0.4); margin-top: 8px; }
.result-cta-btn {
  display: inline-block; padding: 11px 26px; background: var(--accent);
  color: #fff; border-radius: 8px; font-weight: 800; font-size: 0.93rem;
  text-decoration: none; transition: opacity 0.15s;
}
.result-cta-btn:hover { opacity: 0.85; text-decoration: none; }
.result-retry-btn {
  background: transparent; border: 1px solid rgba(255,255,255,0.3);
  color: rgba(255,255,255,0.7); padding: 8px 18px; border-radius: 8px;
  font-size: 0.84rem; font-weight: 600; cursor: pointer;
  font-family: var(--font); margin-top: 12px; transition: all 0.15s;
}
.result-retry-btn:hover { border-color: rgba(255,255,255,0.7); color: #fff; }

/* 렌탈 인포그래픽 */
.result-infographic {
  background: rgba(255,255,255,0.06);
  border-radius: 10px;
  padding: 16px 20px;
  margin: 14px 0;
  text-align: left;
}
.infograph-row {
  display: flex;
  align-items: center;
  gap: 12px;
  margin-bottom: 12px;
}
.infograph-row:last-child { margin-bottom: 0; }
.infograph-label {
  font-size: 0.78rem;
  font-weight: 700;
  color: rgba(255,255,255,0.7);
  white-space: nowrap;
  min-width: 70px;
}
.infograph-bar-track {
  flex: 1;
  background: rgba(255,255,255,0.08);
  border-radius: 999px;
  height: 22px;
  overflow: hidden;
  position: relative;
}
.infograph-bar {
  height: 100%;
  border-radius: 999px;
  transition: width 0.5s ease;
  display: flex;
  align-items: center;
  justify-content: flex-end;
  padding-right: 8px;
  font-size: 0.72rem;
  font-weight: 800;
  color: #fff;
  min-width: 40px;
  box-sizing: border-box;
}
.infograph-bar.rental { background: var(--accent); }
.infograph-bar.buy { background: rgba(255,255,255,0.35); }
.infograph-note {
  font-size: 0.8rem;
  color: rgba(255,255,255,0.6);
  margin: 10px 0 0;
  text-align: center;
}
.infograph-winner {
  font-size: 0.82rem;
  font-weight: 700;
  color: var(--accent);
  text-align: center;
  margin-top: 10px;
}

/* 정보 박스 */
.calc-info-box { background: var(--bg-gray); border-radius: 10px; padding: 20px 24px; margin-bottom: 40px; }
.calc-info-box h3 { font-size: 0.95rem; font-weight: 800; color: var(--primary); margin-bottom: 12px; }
.calc-info-box ul { list-style: none; padding: 0; margin: 0; }
.calc-info-box li { font-size: 0.87rem; color: var(--text-muted); padding: 5px 0 5px 18px; position: relative; line-height: 1.55; }
.calc-info-box li::before { content: '✓'; position: absolute; left: 0; color: var(--accent); font-weight: 700; }

@media (max-width: 600px) {
  .calc-card { padding: 20px 16px; }
  .calc-tab { padding: 8px 12px; font-size: 0.82rem; }
  .calc-row { grid-template-columns: 1fr; }
  .preset-grid { grid-template-columns: repeat(3, 1fr); }
  .elec-preset-grid { grid-template-columns: repeat(3, 1fr); }
  .calc-btn-row { flex-wrap: wrap; }
  .calc-run-btn { max-width: 100%; }
  .infograph-label { min-width: 56px; font-size: 0.72rem; }
}
</style>

<script>
// 탭 전환
document.querySelectorAll('.calc-tab').forEach(tab => {
  tab.addEventListener('click', () => {
    const target = (tab as HTMLElement).dataset.tab;
    document.querySelectorAll('.calc-tab').forEach(t => t.classList.remove('active'));
    document.querySelectorAll('.calc-panel').forEach(p => p.classList.remove('active'));
    tab.classList.add('active');
    document.getElementById(target!)?.classList.add('active');
  });
});
const hash = location.hash.replace('#','');
if (hash) { const tab = document.querySelector(`[data-tab="${hash}"]`) as HTMLElement; if (tab) tab.click(); }

// ① 입주청소 계산기
document.getElementById('ipju-calc-btn')?.addEventListener('click', () => {
  const sizeEl = document.getElementById('ipju-size') as HTMLSelectElement;
  const size = parseInt(sizeEl.value);
  if (!size) { alert('평수를 선택해주세요.'); return; }
  const type = (document.getElementById('ipju-type') as HTMLSelectElement).value;
  const status = parseFloat((document.getElementById('ipju-status') as HTMLSelectElement).value);
  const years = parseFloat((document.getElementById('ipju-years') as HTMLSelectElement).value);
  const dirty = parseFloat((document.querySelector('input[name="ipju-dirty"]:checked') as HTMLInputElement)?.value ?? '1');
  const smoke = parseFloat((document.querySelector('input[name="ipju-smoke"]:checked') as HTMLInputElement)?.value ?? '1');
  const base: Record<number,[number,number]> = {
    10:[100000,180000], 15:[130000,200000], 20:[160000,250000], 25:[220000,330000],
    30:[280000,400000], 33:[320000,450000], 40:[400000,600000], 50:[600000,900000]
  };
  const typeBonus: Record<string,number> = { apt:1, villa:0.92, officetel:0.88 };
  let [low, high] = base[size];
  const mult = dirty * smoke * status * years * typeBonus[type];
  low = Math.round(low * mult / 10000) * 10000;
  high = Math.round(high * mult / 10000) * 10000;
  let addTotal = 0;
  ['add-changtul','add-aircon','add-aircon2','add-hood','add-mold','add-saetak','add-mattress'].forEach(id => {
    const el = document.getElementById(id) as HTMLInputElement;
    if (el?.checked) addTotal += parseInt(el.value);
  });
  if ((document.getElementById('add-veranda1') as HTMLInputElement)?.checked) addTotal += 25000;
  if ((document.getElementById('add-veranda2') as HTMLInputElement)?.checked) addTotal += 50000;
  low += addTotal; high += addTotal;
  const fmt = (n: number) => Math.round(n/10000) + '만원';
  (document.getElementById('ipju-range') as HTMLElement).innerHTML = `예상 비용 <strong>${fmt(low)} ~ ${fmt(high)}</strong>`;
  const details: string[] = [];
  if (dirty > 1) details.push(`오염도 가산 ×${dirty}`);
  if (smoke > 1) details.push(`흡연 가산 ×${smoke}`);
  if (addTotal > 0) details.push(`추가 항목 +${fmt(addTotal)}`);
  (document.getElementById('ipju-detail') as HTMLElement).innerHTML = details.length ? details.join(' · ') : '기본 기준 적용';
  const res = document.getElementById('ipju-result') as HTMLElement;
  res.style.display = 'block';
  res.scrollIntoView({ behavior:'smooth', block:'nearest' });
});

// 입주청소 초기화
document.getElementById('ipju-reset-btn')?.addEventListener('click', () => {
  (document.getElementById('ipju-size') as HTMLSelectElement).value = '';
  (document.getElementById('ipju-type') as HTMLSelectElement).value = 'apt';
  (document.getElementById('ipju-status') as HTMLSelectElement).value = '1.1';
  (document.getElementById('ipju-years') as HTMLSelectElement).value = '1';
  (document.querySelector('input[name="ipju-dirty"][value="1"]') as HTMLInputElement).checked = true;
  (document.querySelector('input[name="ipju-smoke"][value="1"]') as HTMLInputElement).checked = true;
  ['add-changtul','add-veranda1','add-veranda2','add-aircon','add-aircon2','add-hood','add-mold','add-saetak','add-mattress'].forEach(id => {
    (document.getElementById(id) as HTMLInputElement).checked = false;
  });
  (document.getElementById('ipju-result') as HTMLElement).style.display = 'none';
});

document.getElementById('ipju-retry-btn')?.addEventListener('click', () => {
  (document.getElementById('ipju-result') as HTMLElement).style.display = 'none';
  document.getElementById('ipju-calc-btn')?.scrollIntoView({ behavior:'smooth', block:'center' });
});

// ② 렌탈 — 천단위 콤마 자동 포맷
function setupCommaInput(id: string) {
  const el = document.getElementById(id) as HTMLInputElement;
  if (!el) return;
  el.addEventListener('input', () => {
    const pos = el.selectionStart ?? 0;
    const raw = el.value.replace(/[^0-9]/g, '');
    const formatted = raw ? parseInt(raw).toLocaleString('ko-KR') : '';
    el.value = formatted;
    // 커서 위치 보정
    const diff = formatted.length - el.value.length;
    try { el.setSelectionRange(pos + diff, pos + diff); } catch(e) {}
  });
}
setupCommaInput('rental-fee');
setupCommaInput('buy-price');
setupCommaInput('annual-maintenance');

function parseComma(id: string): number {
  const el = document.getElementById(id) as HTMLInputElement;
  return parseInt((el?.value ?? '0').replace(/,/g, '')) || 0;
}

// 렌탈 프리셋
document.querySelectorAll('.preset-btn').forEach(btn => {
  btn.addEventListener('click', () => {
    const el = btn as HTMLElement;
    // 같은 패널의 preset만 selected 처리
    const panel = el.closest('.calc-panel');
    panel?.querySelectorAll('.preset-btn').forEach(b => b.classList.remove('selected'));
    el.classList.add('selected');
    if (el.dataset.fee !== undefined) {
      // 렌탈 프리셋
      const fee = parseInt(el.dataset.fee!);
      const buy = parseInt(el.dataset.buy!);
      const maint = parseInt(el.dataset.maint!);
      (document.getElementById('rental-fee') as HTMLInputElement).value = fee.toLocaleString('ko-KR');
      (document.getElementById('rental-months') as HTMLSelectElement).value = el.dataset.months!;
      (document.getElementById('buy-price') as HTMLInputElement).value = buy.toLocaleString('ko-KR');
      (document.getElementById('annual-maintenance') as HTMLInputElement).value = maint > 0 ? maint.toLocaleString('ko-KR') : '0';
    }
  });
});

// 렌탈 계산기
document.getElementById('rental-calc-btn')?.addEventListener('click', () => {
  const fee = parseComma('rental-fee');
  const months = parseInt((document.getElementById('rental-months') as HTMLSelectElement).value);
  const buyPrice = parseComma('buy-price');
  const annualMaint = parseComma('annual-maintenance');
  const channelBenefit = parseInt((document.querySelector('input[name="rental-channel"]:checked') as HTMLInputElement)?.value ?? '0');
  const ownership = parseInt((document.querySelector('input[name="rental-ownership"]:checked') as HTMLInputElement)?.value ?? '0');
  if (!fee || !buyPrice) { alert('렌탈료와 구매 가격을 입력해주세요.'); return; }
  const ownershipCost = ownership === 2 ? 75000 : 0;
  const totalRental = fee * months - channelBenefit + ownershipCost;
  const totalBuy = buyPrice + (annualMaint * months / 12);
  const totalBuyRound = Math.round(totalBuy);
  const fmt = (n: number) => Math.round(Math.abs(n) / 10000) + '만원';
  let breakEven = 0;
  for (let m=1; m<=200; m++) {
    const rCost = fee * m - channelBenefit + ownershipCost;
    const bCost = buyPrice + annualMaint * m / 12;
    if (bCost < rCost) { breakEven = m; break; }
  }
  const diff = totalRental - totalBuyRound;
  const rangeEl = document.getElementById('rental-range') as HTMLElement;
  if (diff > 0) {
    rangeEl.innerHTML = `${months}개월 기준 구매가 <strong>${fmt(diff)}</strong> 더 저렴`;
  } else if (diff < 0) {
    rangeEl.innerHTML = `${months}개월 기준 렌탈이 <strong>${fmt(Math.abs(diff))}</strong> 더 저렴`;
  } else {
    rangeEl.innerHTML = `${months}개월 기준 렌탈과 구매 비용이 동일합니다`;
  }

  // 인포그래픽
  const maxVal = Math.max(totalRental, totalBuyRound);
  const rentalPct = Math.max(20, Math.round(totalRental / maxVal * 100));
  const buyPct = Math.max(20, Math.round(totalBuyRound / maxVal * 100));
  const infographic = document.getElementById('rental-infographic') as HTMLElement;
  infographic.innerHTML = `
    <div class="result-infographic">
      <div class="infograph-row">
        <span class="infograph-label">렌탈 총비용</span>
        <div class="infograph-bar-track">
          <div class="infograph-bar rental" style="width:${rentalPct}%">${fmt(totalRental)}</div>
        </div>
      </div>
      <div class="infograph-row">
        <span class="infograph-label">구매 총비용</span>
        <div class="infograph-bar-track">
          <div class="infograph-bar buy" style="width:${buyPct}%">${fmt(totalBuyRound)}</div>
        </div>
      </div>
      ${breakEven > 0 ? `<p class="infograph-note">📍 <strong>${breakEven}개월</strong> 이후부터 구매 총비용이 렌탈보다 저렴해집니다</p>` : `<p class="infograph-note">📍 약정 기간(${months}개월) 내에는 구매가 더 유리합니다</p>`}
      <p class="infograph-winner">${diff > 0 ? `✅ 구매가 ${fmt(diff)} 절약` : diff < 0 ? `✅ 렌탈이 ${fmt(Math.abs(diff))} 절약` : '✅ 비용 동일 — 관리 편의성 고려 추천'}</p>
    </div>`;

  // 상세 텍스트
  const detailLines = [
    `렌탈 총비용: ${fmt(totalRental)}${channelBenefit > 0 ? ` (혜택 ${fmt(channelBenefit)} 차감 후)` : ''}`,
    `구매 총비용: ${fmt(totalBuyRound)} (유지비 포함)`,
  ];
  (document.getElementById('rental-detail') as HTMLElement).innerHTML = detailLines.join(' &nbsp;·&nbsp; ');

  // CTA
  const ctaWrap = document.getElementById('rental-cta-wrap') as HTMLElement;
  ctaWrap.innerHTML = `
    <a href="https://partners.ajl.to/lead/rental?partnerId=UDXBJ0IC" rel="nofollow sponsored noopener" data-partner-cta="true" class="result-cta-btn">
      렌탈 알아보기 →
    </a>
    <p class="result-cta-note">* 제휴 링크 · 이용자 추가 비용 없음 · <a href="/affiliate-disclosure/" style="color:inherit;text-decoration:underline">제휴 고지</a></p>`;

  const res = document.getElementById('rental-result') as HTMLElement;
  res.style.display = 'block';
  res.scrollIntoView({ behavior:'smooth', block:'nearest' });
});

// 렌탈 초기화
document.getElementById('rental-reset-btn')?.addEventListener('click', () => {
  (document.getElementById('rental-fee') as HTMLInputElement).value = '';
  (document.getElementById('rental-months') as HTMLSelectElement).value = '60';
  (document.getElementById('buy-price') as HTMLInputElement).value = '';
  (document.getElementById('annual-maintenance') as HTMLInputElement).value = '0';
  (document.querySelector('input[name="rental-channel"][value="0"]') as HTMLInputElement).checked = true;
  (document.querySelector('input[name="rental-ownership"][value="0"]') as HTMLInputElement).checked = true;
  document.querySelectorAll('#rental .preset-btn').forEach(b => b.classList.remove('selected'));
  (document.getElementById('rental-result') as HTMLElement).style.display = 'none';
});

document.getElementById('rental-retry-btn')?.addEventListener('click', () => {
  (document.getElementById('rental-result') as HTMLElement).style.display = 'none';
  document.getElementById('rental-calc-btn')?.scrollIntoView({ behavior:'smooth', block:'center' });
});

// ③ 전기요금 — 가전 프리셋
const elecTips: Record<string, string> = {
  '냉방': '💡 에어컨은 설정 온도 1도 높일수록 약 7% 전력 절감됩니다. 선풍기 병행 시 체감 온도 2~3도 낮아집니다.',
  '난방': '💡 전기히터는 가동 시간 단축이 핵심입니다. 적정 온도 도달 후 끄고 보온재·내복으로 체감 온도를 높이세요.',
  '건조': '💡 건조기 필터를 매 사용 후 청소하면 전력 효율이 유지됩니다. 저온 건조 모드 활용 시 15~25% 절감 가능합니다.',
  '냉장': '💡 냉장고는 24시간 가동되므로 직사광선을 피하고 벽과 10cm 이상 간격을 유지하면 소비전력이 낮아집니다.',
  '조리': '💡 전기밥솥 보온 기능은 전력 소모가 큽니다. 조리 후 바로 끄고 단열 용기에 보관하는 방법을 고려하세요.',
  '영상': '💡 TV 미시청 시 대기전력(0.5~3W)이 계속 소모됩니다. 콘센트를 뽑거나 스마트 멀티탭을 활용하세요.',
  '공조': '💡 공기청정기는 저전력 기기지만 24시간 가동 시 연간 2~4만원 수준입니다. 자동 모드로 두면 효율적입니다.',
  '세탁': '💡 세탁기는 냉수 세탁 전환만으로 온수 가열 비용을 없앨 수 있습니다. 가득 채워 1회 세탁이 소량 여러 번보다 효율적입니다.',
};

document.querySelectorAll('.elec-preset').forEach(btn => {
  btn.addEventListener('click', () => {
    const el = btn as HTMLElement;
    document.querySelectorAll('.elec-preset').forEach(b => b.classList.remove('selected'));
    el.classList.add('selected');
    const watt = el.dataset.watt!;
    const hours = el.dataset.hours!;
    const days = el.dataset.days!;
    const tip = el.dataset.tip!;
    // watt select 매칭
    const wattSel = document.getElementById('elec-watt') as HTMLSelectElement;
    const opts = Array.from(wattSel.options).map(o => o.value);
    if (opts.includes(watt)) wattSel.value = watt;
    (document.getElementById('elec-watt-custom') as HTMLInputElement).value = '';
    (document.getElementById('elec-hours') as HTMLInputElement).value = hours;
    (document.getElementById('elec-days') as HTMLInputElement).value = days;
    const tipBox = document.getElementById('elec-tip-box') as HTMLElement;
    const tipText = document.getElementById('elec-tip-text') as HTMLElement;
    if (elecTips[tip]) {
      tipText.textContent = elecTips[tip];
      tipBox.style.display = 'block';
    } else {
      tipBox.style.display = 'none';
    }
  });
});

// 전기요금 계산기
document.getElementById('jeongi-calc-btn')?.addEventListener('click', () => {
  const customWatt = parseInt((document.getElementById('elec-watt-custom') as HTMLInputElement).value);
  const selectedWatt = parseInt((document.getElementById('elec-watt') as HTMLSelectElement).value);
  const watt = customWatt > 0 ? customWatt : selectedWatt;
  const hours = parseFloat((document.getElementById('elec-hours') as HTMLInputElement).value);
  const days = parseInt((document.getElementById('elec-days') as HTMLInputElement).value);
  const price = parseInt((document.getElementById('elec-price') as HTMLSelectElement).value);
  if (!watt || !hours || !days) { alert('소비전력, 사용 시간, 사용 일수를 입력해주세요.'); return; }
  const kwhPerMonth = watt / 1000 * hours * days;
  const cost = Math.round(kwhPerMonth * price);
  const annualCost = cost * 12;
  (document.getElementById('jeongi-range') as HTMLElement).innerHTML =
    `월 추가 전기요금 약 <strong>${cost.toLocaleString('ko-KR')}원</strong>`;
  const yearlyStr = annualCost >= 10000 ? `연간 약 ${Math.round(annualCost/10000)}만원` : `연간 약 ${annualCost.toLocaleString('ko-KR')}원`;
  (document.getElementById('jeongi-detail') as HTMLElement).innerHTML =
    `월 소비 전력량 ${kwhPerMonth.toFixed(1)} kWh &nbsp;·&nbsp; ${yearlyStr}`;
  const noteEl = document.getElementById('jeongi-note') as HTMLElement;
  noteEl.textContent = customWatt > 0
    ? `직접 입력한 ${customWatt}W 기준으로 계산되었습니다. 실제 요금은 가구 전체 사용량·누진 구간에 따라 달라집니다.`
    : `실제 전기요금은 가구별 전체 사용량·누진 구간에 따라 달라집니다. 이 값은 해당 가전제품 단독 추가분입니다.`;
  const res = document.getElementById('jeongi-result') as HTMLElement;
  res.style.display = 'block';
  res.scrollIntoView({ behavior:'smooth', block:'nearest' });
});

// 전기요금 초기화
document.getElementById('jeongi-reset-btn')?.addEventListener('click', () => {
  (document.getElementById('elec-watt') as HTMLSelectElement).value = '1500';
  (document.getElementById('elec-watt-custom') as HTMLInputElement).value = '';
  (document.getElementById('elec-hours') as HTMLInputElement).value = '6';
  (document.getElementById('elec-days') as HTMLInputElement).value = '30';
  (document.getElementById('elec-price') as HTMLSelectElement).value = '185';
  document.querySelectorAll('.elec-preset').forEach(b => b.classList.remove('selected'));
  (document.getElementById('elec-tip-box') as HTMLElement).style.display = 'none';
  (document.getElementById('jeongi-result') as HTMLElement).style.display = 'none';
});

document.getElementById('jeongi-retry-btn')?.addEventListener('click', () => {
  (document.getElementById('jeongi-result') as HTMLElement).style.display = 'none';
  document.getElementById('jeongi-calc-btn')?.scrollIntoView({ behavior:'smooth', block:'center' });
});
</script>

</Layout>
